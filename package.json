{
  "name": "scheduler",
  "version": "1.0.0",
  "description": "Tutoring Scheduler — monorepo root",
  "private": true,
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
